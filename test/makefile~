CPP_SRCS = pattern_circle.cpp pattern_rect.cpp

MAIN = output

OBJS = pattern_circle.o pattern_rect.o

all : run

run : $(MAIN)
	export LD_LIBRARY_PATH=../lib/:$ LD_LIBRARY_PATH && ./$(MAIN)

$(MAIN) : $(CPP_SRCS)
	g++ -L../lib/ -Wall -o $(MAIN) $(CPP_SRCS) -ldxf_2D


